export type User = {
  name: string;
  avatar: string;
  whatsapp: string;
  bio: string;
  id: string;
};

export type Class = {
  subject: string;
  cost: number;
};

export type ScheduleItem = {
  week_day: number;
  from: string;
  to: string;
};


